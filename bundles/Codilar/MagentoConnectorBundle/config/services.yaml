services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    Codilar\MagentoConnectorBundle\Controller\:
        resource: '../src/Controller'
        public: true
        tags: ['controller.service_arguments']
    
    Codilar\MagentoConnectorBundle\Tool\Installer:
        public: true

    codilar_magento_connector.event_listener:
        class: Codilar\MagentoConnectorBundle\EventListener\DataObjectEventListener
        tags:
            - { name: kernel.event_listener, event: pimcore.dataobject.postUpdate, method: onPostUpdate }

    Codilar\MagentoConnectorBundle\Service\ApiService:
        arguments:
            $apiKey: '%env(API_KEY)%'
        public: true

# add more services, or override services that need manual wiring
#    Codilar\MagentoConnectorBundle\ExampleClass:
#        arguments:
#            - "@service_id"
#            - "plain_value"
#            - "%parameter%"
